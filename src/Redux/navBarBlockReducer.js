const initialState = {
    Friends: [
        {id: 0, name: 'Friend 1'},
        {id: 1, name: 'Friend 2'},
        {id: 2, name: 'Friend 3'},
    ],
}

const navBarBlockReducer = (state = initialState, action) => {
    return state;
}

export default navBarBlockReducer;